name: hello world

on:
  pull_request:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest

  steps:
    - name: Checkout-code
      uses: action/checkout@v2

    - name: Say hello world
      run: echo "hello world"
